$(document).ready(function(){
    $("button").click(function(){
      // Oculta todo
      //$("*").hide();
      
      // Oculta el botón
      //$(this).hide();

      // Oculta el primer párrafo con la clase "intro"
      //$("p.intro").hide();

      // Oculta el primer párrafo utilizando p:first
      //$("p:first").hide();

      
    });
  });